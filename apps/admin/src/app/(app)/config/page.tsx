import { ComingSoon } from "../../../components/ComingSoon";

export default function ConfigPage() {
  return <ComingSoon title="Config" description="Platform config (fees, discounts, surcharges) — not spec'd yet." />;
}
