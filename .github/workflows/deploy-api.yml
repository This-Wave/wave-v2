# API deploys on Render via render.yaml (Blueprint or connected repo).
# Railway auto-deploy was retired 2026-08-11 to avoid dual-host drift (checklist C9).
name: Deploy API (Render — manual reference only)

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Wave API deploys on Render, not Railway."
          echo "See render.yaml and README.md → Deployment."
