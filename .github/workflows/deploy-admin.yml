# Admin deploys on Render via render.yaml (same Blueprint as the API).
# Vercel auto-deploy was retired 2026-08-11 — admin + API share one Render project.
name: Deploy Admin (Render — manual reference only)

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Wave admin deploys on Render, not Vercel."
          echo "See render.yaml and README.md → Deployment."
