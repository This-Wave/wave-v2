# Neon.tech — PostgreSQL 16, never Supabase DB (it pauses after 7 days idle)
DATABASE_URL=postgresql://user:pass@ep-xxx.neon.tech/wave?sslmode=require
# Same database, non-pooled endpoint (drop `-pooler` and `channel_binding`).
# `prisma migrate` cannot use the pooler.
DIRECT_URL=
