{
  "name": "@wave/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod schemas, types, and constants used across api, admin, mobile",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "files": [
    "dist"
  ]
}
